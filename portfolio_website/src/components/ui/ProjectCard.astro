---
import type { Project } from "../../types/content";
import Button from "./Button.astro";

type Props = {
  project: Project;
};

const { project } = Astro.props;
---

<!-- Project card: one reusable visual unit for every item in projects.ts. -->
<article class="surface-card project-card">
  <h3 class="project-card-title">{project.title}</h3>
  <p class="project-card-description">{project.description}</p>

  <ul class="tag-list project-card-stack">
    {project.stack.map((item) => (
      <li class="tag">{item}</li>
    ))}
  </ul>

  <div class="actions-row project-card-actions">
    <Button href={project.liveUrl} label="Live Demo" variant="primary" />
    <Button href={project.repoUrl} label="View Code" variant="secondary" />
  </div>
</article>
