---
type Props = {
  className?: string;
};

const { className = "" } = Astro.props;
const rowClass = ["flex flex-wrap gap-4", className].filter(Boolean).join(" ");
---

<div class={rowClass}>
  <slot />
</div>
