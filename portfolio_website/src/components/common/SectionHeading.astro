---
type Props = {
  label: string;
  title: string;
};

const { label, title } = Astro.props;
---

<div class="mb-6">
  <p class="mb-2 font-semibold text-portfolio-primary">{label}</p>
  <h2 class="mb-4 text-[2rem] text-portfolio-text">{title}</h2>
</div>
